# Runtime dependencies for the SHACL validator container.
#
# The RDF stack is pinned to the SAME versions as the community validibot repo
# (validibot/pyproject.toml) so the container's parsing/validation behaviour
# matches what authors test against. Keep these in lockstep on every bump.
pydantic==2.13.3
httpx==0.28.1
google-cloud-storage==3.10.1
google-auth==2.50.0
defusedxml==0.7.1

# RDF / SHACL stack (matches validibot community pins).
rdflib==7.6.0
pyshacl==0.31.0
owlrl==7.1.4
# lxml: kept >=6.1.0 (CVE-2026-41066 XXE fix). Not on the SHACL critical path
# — rdflib uses its own SAX parser and we layer defusedxml + the XXE regex in
# front — but it ships as an rdflib/lxml dependency, so we pin the patched ver.
lxml==6.1.1

# Validibot shared library (from PyPI).
# Pin exact version to bust Docker layer cache on upgrades.
validibot-shared==0.9.0
